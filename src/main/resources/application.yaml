spring:
  application:
    name: ProductService
  data:
    mongodb:
      uri: mongodb://localhost:27017/product-db
  security:
    oauth2:
      resourceserver:
        jwt:
          issuer-uri: "http://localhost:9090/realms/owu-ecommerce"

server:
  port: 8080